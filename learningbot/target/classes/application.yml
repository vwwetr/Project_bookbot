# ===============================
# 🔧 Telegram Bot configuration
# ===============================
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

telegram:
  bot:
    username: vwwetrbot               # имя, которое ты задал в BotFather
    token: 8245022377:AAEnGLfk2PXewHpqpEQFajyd1uUysepcz7A # токен можно задать прямо или через переменную окружения

# ===============================
# 🗄️ Database (PostgreSQL)
# ===============================
#spring:
  #datasource:
   # url: jdbc:postgresql://localhost:5432/resources   # адрес твоей локальной БД
    #username: bot_user                                # пользователь БД
    #password: bot_pass                                # пароль БД
   # driver-class-name: org.postgresql.Driver

#  jpa:
#    hibernate:
#      ddl-auto: update        # автоматически создаёт/обновляет таблицы
#    show-sql: true            # показывает SQL-запросы в консоли
#    properties:
#      hibernate:
#        format_sql: true      # форматирует SQL для удобства

# ===============================
# 🌐 Server
# ===============================
server:
  port: 8080                  # порт, на котором работает приложение

# ===============================
# 🩺 Actuator & monitoring
# ===============================
management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

# ===============================
# 🧾 Logging
# ===============================
logging:
  file:
    path: logs/
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
    com.learningbot: DEBUG